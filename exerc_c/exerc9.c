#include <stdio.h>

//Faça um programa para calcular a área de um triângulo (ponto flutuante).


int main (){
	float base, altura, area;
	base = 10;
	altura = 7;
	area = (base * altura)/2;
	printf("A area do triangulo é %.2f:", area);
	
}
